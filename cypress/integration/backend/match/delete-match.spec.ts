describe("remove match", () => {
  it("provides all parameters and receive success", () => {

  });

  describe("deletes same match two times", () => {
    it("and fails", () => {});
  });

  describe("provides inexistent match", () => {
    it("and fails", () => {});
  });
});
